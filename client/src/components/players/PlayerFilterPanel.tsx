import React from 'react';

/** Placeholder until filters are wired to roster data in this tab layout. */
export const PlayerFilterPanel: React.FC = () => (
  <div className="players-tab__filters">
    <p style={{ color: '#666', fontSize: 14 }}>No filters in this preview layout.</p>
  </div>
);
